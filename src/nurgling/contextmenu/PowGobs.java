package nurgling.contextmenu;

final class PowGobs {
    static final String NAME = "gfx/terobjs/pow";

    static boolean matches(String name) {
        return NAME.equals(name);
    }

    private PowGobs() {}
}
